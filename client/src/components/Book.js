import { ApolloClient, ApolloLink, InMemoryCache } from "@apollo/client";

function Book() {
    return (
      <div className="Book">
        Hello Book
      </div>
    );
  }
  
  export default Book;  